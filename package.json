{
  "name": "ufo-simulator-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "npx @biomejs/biome check .",
    "preview": "vite preview",
    "prepare": "husky"
  },
  "dependencies": {
    "pixi.js": "^8.17.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.2"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,css,json}": "biome check --write --no-errors-on-unmatched"
  }
}
